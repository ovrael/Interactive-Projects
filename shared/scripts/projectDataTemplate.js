class ProjectData {

    static SettingsName = 'TemplateName';

    // tab: TAB_NAME
    // section: SECTION_NAME
    static TemplateVariable = data;         // TYPE, OPTIONS

    // section:NEXT_SECTION_NAME
    static TemplateSlider = 50;             // slider,   SLIDER_NAME,   MIN, MAX, DEFAULT, STEP
    static TemplateColor = '#646464';       // color,    COLOR_NAME,    HEX_COLOR
    static TemplateCheckbox = true;         // checkbox, CHECKBOX_NAME, DEFAULT
    static TemplateSelect = 'triangle';     // select,   SELECT_NAME,   OPTION_1, OPTION_2...

    static TemplateCommentedVariable = 0;   /* Interesting comment */
}